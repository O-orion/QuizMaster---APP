import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
        borderRadius: 12,
        overflow: "hidden",
        marginVertical: 10,
        width: '100%'
      },
      gradient: {
        alignItems: "center",
        padding: 15
      },
      buttonText: {
        fontFamily: "InterBold",
        fontSize: 18,
        color: "#FFFFFF",
      },
})