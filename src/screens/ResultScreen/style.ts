import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    score: {
        fontFamily: "InterBold",
        fontSize: 64,
        color: "#6B48FF",
        textAlign: "center",
        marginVertical: 20,
        textShadowColor: "rgba(107, 72, 255, 0.3)",
        textShadowOffset: { width: 2, height: 2 },
        textShadowRadius: 8,
      },
})
